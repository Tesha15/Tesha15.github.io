"use strict";



window.addEventListener("load", function() {
	var addButton = document.forms.addButton;
	




 
 
	

    // Get product details
    var productName = productContainer.querySelectorAll("h2, h3, h4, h5");
    var productImage = productContainer.querySelectorAll("img");
    var productPrice = productContainer.querySelectorAll("p");

    // Create cart
    var cartItem = {
      name: productName,
      image: productImage,
      price: productPrice,
     };
  });
}